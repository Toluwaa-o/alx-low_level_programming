/*
 * add - Add two integers and return thr result.
 * Description - a function that adds two integers 
 * 		and returns the result.
 */

int add(int x, int y)
{
	int result = x + y;
	return (result);
}
